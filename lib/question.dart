class Question {
  String questionText;
  bool questionAnswer;

  Question(String q, bool a)
      : questionText = q,
        questionAnswer = a;
}

// Example of creating an instance of Question
// Question myQuestion = Question("Is Dart strongly typed?", true);
